---
title: Permissions 权限
description: 配置 Codex 权限配置文件，精细控制文件系统和网络访问。
sidebar:
  order: 18
---

import LinkButton from '@lucode-starlight/components/custom/LinkButton.astro';

# Permissions 权限

> 配置 Codex 权限配置文件，控制文件和网络访问

:::caution[Beta 功能]
权限配置文件正在积极开发中，可能发生变化。
:::

权限配置文件（Permission profiles）让你对 Codex 代表你运行的本地命令应用最小权限边界。一个配置文件是命名策略，结合了定义命令可读取或写入的文件系统规则，以及定义命令可访问的目的地的网络规则。

Codex 包含三个内置权限配置文件：

| 内置配置 | 说明 |
| --- | --- |
| `:read-only` | 保持本地命令执行为只读 |
| `:workspace` | 允许在工作区根目录内写入 |
| `:danger-full-access` | 移除所有本地沙箱限制 |

## 定义和选择配置文件

```toml
default_permissions = "project-edit"

[permissions.project-edit.workspace_roots]
"~/code/app" = true
"~/code/shared-lib" = true

[permissions.project-edit.filesystem]
":minimal" = "read"

[permissions.project-edit.filesystem.":workspace_roots"]
"." = "write"
".devcontainer" = "read"
"**/*.env" = "deny"

[permissions.project-edit.network]
enabled = true

[permissions.project-edit.network.domains]
"api.openai.com" = "allow"
"*.github.com" = "allow"
"tracking.example.com" = "deny"
```

此配置文件：
- 读取常用开发者工具所需的最少运行时路径
- 对当前会话和配置文件定义的工作区根目录应用相同的工作区规则
- 保持 IDE 相邻设置（如 `.devcontainer/`）为只读
- 使用 glob 规则拒绝匹配的环境文件
- 仅通过配置的域名策略允许网络访问

## 文件系统权限

| 访问级别 | 含义 |
| --- | --- |
| `read` | 允许读取文件和列出目录，不能创建、修改、重命名或删除 |
| `write` | 允许读取和修改文件，包括创建、重命名和删除 |
| `deny` | 拒绝读取和写入，用于从更广泛的授权中挖出禁止子路径 |

## 网络权限

```toml
[permissions.project-edit.network]
enabled = true

[permissions.project-edit.network.domains]
"example.com" = "allow"       # 精确主机
"*.example.com" = "allow"     # 子域名
"**.example.com" = "allow"    # 顶级和子域名
"ads.example.com" = "deny"    # 拒绝优先于允许
```

## 常见配置示例

**只读 + 网络白名单：**
```toml
default_permissions = "readonly-net"

[permissions.readonly-net.filesystem]
":minimal" = "read"
[permissions.readonly-net.filesystem.":workspace_roots"]
"." = "read"
[permissions.readonly-net.network]
enabled = true
[permissions.readonly-net.network.domains]
"api.openai.com" = "allow"
```

**工作区写入 + 无网络：**
```toml
default_permissions = "project-edit"

[permissions.project-edit.filesystem]
":minimal" = "read"
[permissions.project-edit.filesystem.":workspace_roots"]
"." = "write"
[permissions.project-edit.network]
enabled = false
```

<LinkButton href="https://developers.openai.com/codex/configuration/permissions" variant="secondary">
  查看官方完整文档 →
</LinkButton>

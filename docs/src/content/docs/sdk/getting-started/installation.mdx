---
title: Installation
description: Install the SDK and verify the second docset renders with its own sidebar.
---

# Installation

Install the SDK with your package manager of choice:

```bash
bun add @acme/sdk
```

Once installed, continue to the [client configuration guide](/sdk/reference/client-configuration/)
to wire a runtime client.

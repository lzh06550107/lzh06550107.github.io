---
title: 开始 ComfyUI 的 AI 绘图之旅
description: 本部分教程将会带你完成首次 ComfyUI 的图片生成，了解并熟悉 ComfyUI 中的一些界面基础操作，如工作流加载、模型安装、图片生成等
sidebar:
  label: 开始 AI 绘图之旅
---

# 开始 ComfyUI 的 AI 绘图之旅

本部分文档仅针对**本地用户**，即在自己设备上安装并运行 ComfyUI 的用户。如果你不想进行本地安装，可以直接使用官方云服务。无需安装，直接在云端运行 ComfyUI。使用 Google 账号登录，每位用户**每月可获得 400 积分**。

本篇的主要目的是带你初步了解 ComfyUI，熟悉 ComfyUI 的一些基础操作，并引导你完成首次图片生成：

1. 加载示例工作流
   - 从 ComfyUI 加载 `Workflows template` 中的 `Text to Image` 工作流
   - 使用带有 `metadata` 的图片中加载工作流
2. 完成模型安装
   - 自动安装模型
   - 手动安装模型
3. 进行一次文本到图片的生成

## 关于文生图的说明

**文生图（Text to Image）** 是 AI 绘图的基础，通过输入文本描述来生成对应的图片，是 AI 绘图最常用的功能之一。你可以理解成你把你的**绘图要求（正向提示词、负向提示词）**告诉一个**画家（绘图模型）**，画家会根据你的要求画出你想要的内容。

## ComfyUI 文生图工作流教程讲解

### 1. 启动 ComfyUI

请确定你已经按照安装指南完成了 ComfyUI 的启动，并可以成功打开 ComfyUI 页面。或者，你也可以使用 Comfy Cloud 在云端使用 ComfyUI，无需安装。

### 2. 加载默认文生图工作流

正常情况下，打开 ComfyUI 后会自动加载默认的文生图工作流。不过你仍旧可以尝试以下不同方式加载工作流来熟悉 ComfyUI 的一些基础操作：

1. 点击 ComfyUI 界面右下角的 **Fit View** 按钮，防止已加载工作流在视图外导致不可见
2. 点击侧边栏的 **文件夹图标（workflows）**
3. 点击工作流面板顶部的 **浏览工作流示例（Browse example workflows）** 按钮
4. 选择默认的第一个工作流 **Image Generation**

所有用 ComfyUI 生成的图片都会带有 metadata 信息，其中会包含 workflow 信息。你可以通过这些信息加载对应的 workflow。

ComfyUI 工作流还可以用 JSON 格式存储。当你完成一个工作流后，可以使用菜单 **工作流（Workflows）** → **导出（Export）** 导出，这样对应的工作流就可以被保存为 JSON 文件中加载。

### 3. 安装绘图模型

通常在 ComfyUI 的初始安装中，并不会包含任何绘图模型，但模型是运行图片生成必不可少的部分。

在你完成第二步、工作流加载后，如果你的电脑上没有安装 `v1-5-pruned-emaonly-fp16.safetensors` 这个模型文件，一般会出现模型缺失提示。

无论使用哪种安装方法，模型都会被保存到：

`<你的 ComfyUI 安装位置>/ComfyUI/models/`

你会看到很多按用途分类的子文件夹，例如：

- `checkpoints`
- `embeddings`
- `vae`
- `lora`
- `upscale_model`

ComfyUI 桌面版的模型安装位置可能与其它版本不同。按下面步骤打开模型文件夹：

1. 点击左上角 **应用图标（Logo）**
2. 打开 **帮助** → **打开文件夹**
3. 选择 **打开模型文件夹**

便携版的模型文件夹一般在：

`ComfyUI_windows_portable/ComfyUI/models/`

### 4. 加载模型，并进行第一次图片生成

在你完成模型的安装或更新后，可以按键盘上的 **`R`** 键刷新对象定义，从而更新节点里的模型列表（例如 **Load Checkpoint** 下拉框中的可选模型）。

1. 请在 **Load Checkpoint** 节点中确保 `v1-5-pruned-emaonly-fp16.safetensors` 被选中，且左右切换箭头不会出现 `null`
2. 点击 `Run` 按钮，或者使用快捷键 `Ctrl + Enter` 来执行图片生成

等待对应流程执行完成后，你应该可以在界面的 **保存图像（Save Image）** 节点中看到对应的图片结果。

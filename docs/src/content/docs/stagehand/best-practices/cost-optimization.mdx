---
title: 成本优化
description: 在保持自动化效果的同时，降低 Stagehand 的 LLM 推理成本与浏览器基础设施成本。
editUrl: false
sidebar:
  order: 3
---

import { Card, CardGrid } from '@astrojs/starlight/components';

> **文档索引**
>
> 可在此获取完整文档索引：[https://docs.stagehand.dev/llms.txt](https://docs.stagehand.dev/llms.txt)
>
> 在进一步浏览前，可使用该文件发现所有可用页面。

# 成本优化

> 在保持自动化性能的同时尽可能降低成本

Stagehand 中的成本优化，核心是在 LLM 推理成本与浏览器基础设施成本之间取得平衡。本指南提供了一些可直接落地的实用策略，帮助你减少自动化开销。

## 快速见效的方法

先从这些简单的优化开始，它们通常就能帮助你显著降低成本。

### 1. 为任务选择合适的模型

对于简单任务，我们不建议使用更大、更昂贵的高级模型。你可以查看我们的[评测结果](https://stagehand.dev/evals)，比较不同任务类型下各模型的性能与成本。

<CardGrid>
  <Card title="模型选择指南" icon="brain" href="/stagehand/configuration/models/">
    根据你的预算和准确率要求选择合适的 LLM。
  </Card>

  <Card title="评测结果" icon="chart-line" href="https://www.stagehand.dev/evals">
    查看不同模型在不同任务中的表现。
  </Card>
</CardGrid>

### 2. 实现缓存

启用自动操作缓存，可以消除重复的 LLM 调用。只需在初始化 Stagehand 时指定 `cacheDir`：

```ts
const stagehand = new Stagehand({
  env: "BROWSERBASE",
  cacheDir: "action-cache", // 启用自动缓存
});

await stagehand.init();

// 首次运行：使用 LLM 推理并写入缓存
// 后续运行：复用已缓存的操作（不再产生 LLM 成本）
await stagehand.act("Click the sign in button");
```

<CardGrid>
  <Card title="缓存指南" icon="database" href="/stagehand/best-practices/caching/">
    了解如何组织缓存以及管理缓存目录。
  </Card>
</CardGrid>

### 3. 优化浏览器会话

在可能的情况下复用会话，并设置合适的超时时间。更多细节可参考[浏览器配置](/stagehand/configuration/browser/)：

```ts
const stagehand = new Stagehand({
  env: "BROWSERBASE",
  browserbaseSessionCreateParams: {
    timeout: 1800, // 30 分钟，而不是默认的 1 小时
    keepAlive: true, // 在任务之间保持会话存活
  }
});
```

<CardGrid>
  <Card title="Browserbase 成本优化" icon="window-maximize" href="https://docs.browserbase.com/guides/cost-optimization">
    优化 Browserbase 基础设施成本与会话管理。
  </Card>
</CardGrid>

## 高级策略

### 智能模型切换

对于简单任务，自动回退到更便宜的模型：

```ts
// 根据任务复杂度，从低成本模型逐步回退到高成本模型
// 性能对比请参见 stagehand.dev/evals
async function smartAct(prompt: string) {
  const models = ["google/gemini-2.5-flash", "openai/gpt-4o"];

  for (const model of models) {
    try {
      const stagehand = new Stagehand({
        env: "LOCAL",
        model: model
      });
      await stagehand.init();
      const [action] = await stagehand.observe(prompt);
      await stagehand.act(action);
      await stagehand.close();
      return;
    } catch (error) {
      console.log(`Falling back to ${model}...`);
      await stagehand.close();
    }
  }
}
```

### 会话池化

在多个任务之间复用浏览器会话：

```ts
class SessionManager {
  private sessions = new Map<string, Stagehand>();
  
  async getSession(taskType: string): Promise<Stagehand> {
    if (this.sessions.has(taskType)) {
      return this.sessions.get(taskType)!;
    }
    
    const stagehand = new Stagehand({ env: "BROWSERBASE" });
    await stagehand.init();
    this.sessions.set(taskType, stagehand);
    return stagehand;
  }
}
```

## 成本监控

跟踪你的花费，有助于发现优化空间。详细指标请参见[可观测性指南](/stagehand/configuration/observability/)：

```ts
// 监控 token 使用量
const metrics = await stagehand.metrics;
console.log(`Total tokens: ${metrics.totalPromptTokens + metrics.totalCompletionTokens}`);
console.log(`Estimated cost: $${(metrics.totalPromptTokens + metrics.totalCompletionTokens) * 0.00001}`);
```

<CardGrid>
  <Card title="可观测性与指标" icon="chart-line" href="/stagehand/configuration/observability/">
    实时监控使用模式并跟踪成本。
  </Card>
</CardGrid>

## 预算控制

设置支出上限，避免产生意外成本：

```ts
class BudgetGuard {
  private dailySpend = 0;
  private maxDailyBudget: number;
  
  constructor(maxDailyBudget: number = 25) {
    this.maxDailyBudget = maxDailyBudget;
  }
  
  checkBudget(estimatedCost: number): void {
    if (this.dailySpend + estimatedCost > this.maxDailyBudget) {
      throw new Error(`Daily budget exceeded: $${this.maxDailyBudget}`);
    }
    this.dailySpend += estimatedCost;
  }
}
```

## 相关资源

<CardGrid>
  <Card title="模型选择指南" icon="brain" href="/stagehand/configuration/models/">
    根据你的预算和准确率要求选择合适的 LLM。
  </Card>

  <Card title="缓存策略" icon="database" href="/stagehand/best-practices/caching/">
    通过智能操作缓存与观察模式来降低成本。
  </Card>

  <Card title="可观测性与指标" icon="chart-line" href="/stagehand/configuration/observability/">
    监控使用模式并实时跟踪成本。
  </Card>

  <Card title="浏览器配置" icon="window-maximize" href="/stagehand/configuration/browser/">
    优化 Browserbase 基础设施成本与会话管理。
  </Card>
</CardGrid>
